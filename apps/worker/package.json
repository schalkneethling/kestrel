{
  "name": "@kestrel/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply kestrel --local",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "@kestrel/core": "workspace:*",
    "@varlock/cloudflare-integration": "^1.2.1",
    "varlock": "^1.10.0"
  }
}
